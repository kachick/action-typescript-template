name: Format
on:
  push:
    branches: [main]
    paths-ignore:
      - 'dist'
  pull_request:
    paths-ignore:
      - 'dist'
jobs:
  dprint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dprint/check@v2.2
